// Benchmark: Recursive Fibonacci
// Measures function call overhead + recursion depth

fn fib(n: i64) -> i64 {
    if n <= 1 { n }
    else { fib(n - 1) + fib(n - 2) }
}

fn main() -> void {
    let result = fib(30)
    println(f"fib(30) = {result}")
}
