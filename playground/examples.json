[
  {"name": "Hello World", "description": "Basic hello world program", "code": "fn main() {\n    println(\"Hello, Fajar Lang!\")\n}"},
  {"name": "Fibonacci", "description": "Recursive Fibonacci", "code": "fn fib(n: i64) -> i64 {\n    if n <= 1 { n } else { fib(n-1) + fib(n-2) }\n}\nfn main() {\n    println(fib(20))\n}"},
  {"name": "Effect System", "description": "Formal effect system", "code": "effect Logger {\n    fn log(msg: str) -> void\n}\n\nfn greet() with IO {\n    println(\"Hello with effects!\")\n}\n\nfn main() { greet() }"},
  {"name": "Comptime", "description": "Compile-time evaluation", "code": "comptime fn factorial(n: i64) -> i64 {\n    if n <= 1 { 1 } else { n * factorial(n - 1) }\n}\n\nfn main() {\n    let result = comptime { factorial(10) }\n    println(result)\n}"},
  {"name": "Pattern Matching", "description": "Pattern matching", "code": "fn describe(n: i64) -> str {\n    match n {\n        0 => \"zero\",\n        1 => \"one\",\n        _ => \"other\"\n    }\n}\nfn main() {\n    println(describe(0))\n    println(describe(42))\n}"},
  {"name": "Macros", "description": "Built-in macros", "code": "fn main() {\n    let arr = vec![1, 2, 3, 4, 5]\n    let msg = concat!(\"length: \", len(arr))\n    println(msg)\n}"},
  {"name": "Structs", "description": "Struct types", "code": "struct Point {\n    x: f64,\n    y: f64\n}\n\nfn distance(p: Point) -> f64 {\n    sqrt(p.x * p.x + p.y * p.y)\n}\n\nfn main() {\n    let p = Point { x: 3.0, y: 4.0 }\n    println(distance(p))\n}"},
  {"name": "Context Safety", "description": "Compiler-enforced context isolation", "code": "@kernel fn read_hw() -> i64 {\n    // Only hardware ops allowed here\n    0\n}\n\n@device fn inference() -> i64 {\n    // Only tensor ops allowed here\n    42\n}\n\n@safe fn bridge() -> i64 {\n    // Safest: no hardware, no tensor\n    0\n}\n\nfn main() {\n    println(\"Context annotations enforce safety!\")\n}"},
  {"name": "Hello World", "description": "Basic hello world with variables and function calls", "code": "fn main() {\n    let name: str = \"Fajar Lang\"\n    let version = 1.1\n    println(f\"Hello, {name} v{version}!\")\n\n    let x: i32 = 42\n    let y = x * 2\n    println(f\"The answer is {y}\")\n}", "difficulty": "Beginner", "category": "basics"},
  {"name": "Pattern Matching", "description": "Enum definition and exhaustive match expressions", "code": "enum Color {\n    Red,\n    Green,\n    Blue,\n    Custom(i32, i32, i32),\n}\n\nfn describe(c: Color) -> str {\n    match c {\n        Color::Red => \"warm red\",\n        Color::Green => \"lush green\",\n        Color::Blue => \"deep blue\",\n        Color::Custom(r, g, b) => f\"rgb({r},{g},{b})\",\n    }\n}\n\nfn main() {\n    println(describe(Color::Red))\n    println(describe(Color::Custom(255, 128, 0)))\n}", "difficulty": "Beginner", "category": "basics"},
  {"name": "Structs & Methods", "description": "Point struct with impl block and method calls", "code": "struct Point {\n    x: f64,\n    y: f64,\n}\n\nimpl Point {\n    fn new(x: f64, y: f64) -> Point {\n        Point { x: x, y: y }\n    }\n\n    fn distance(self, other: Point) -> f64 {\n        let dx = self.x - other.x\n        let dy = self.y - other.y\n        sqrt(dx * dx + dy * dy)\n    }\n\n    fn translate(self, dx: f64, dy: f64) -> Point {\n        Point { x: self.x + dx, y: self.y + dy }\n    }\n}\n\nfn main() {\n    let a = Point::new(0.0, 0.0)\n    let b = Point::new(3.0, 4.0)\n    println(f\"Distance: {a.distance(b)}\")\n}", "difficulty": "Beginner", "category": "basics"},
  {"name": "Error Handling", "description": "Result type, ? operator, and error propagation", "code": "fn parse_age(input: str) -> Result<i32, str> {\n    let n = parse_int(input)\n    match n {\n        Ok(age) => {\n            if age < 0 { Err(\"age cannot be negative\") }\n            else if age > 150 { Err(\"unrealistic age\") }\n            else { Ok(age) }\n        },\n        Err(_) => Err(\"not a valid number\"),\n    }\n}\n\nfn main() {\n    let inputs = [\"25\", \"-3\", \"abc\", \"200\"]\n    for input in inputs {\n        match parse_age(input) {\n            Ok(age) => println(f\"{input} -> valid age: {age}\"),\n            Err(msg) => println(f\"{input} -> error: {msg}\"),\n        }\n    }\n}", "difficulty": "Intermediate", "category": "safety"},
  {"name": "Tensor Operations", "description": "Create tensors, matmul, activation, shape manipulation", "code": "@device\nfn tensor_demo() {\n    let a = zeros(3, 3)\n    let b = ones(3, 3)\n    let c = randn(3, 3)\n\n    let sum = a + b\n    let product = matmul(b, c)\n\n    let activated = relu(product)\n    let normalized = softmax(activated)\n\n    println(f\"Shape: {normalized.shape()}\")\n    println(f\"Sum: {normalized.sum()}\")\n}", "difficulty": "Intermediate", "category": "ml"},
  {"name": "ML Training", "description": "Simple linear regression with autograd and optimizer", "code": "@device\nfn train_linear() {\n    // y = 2x + 1 (target)\n    let x = from_data([[1.0], [2.0], [3.0], [4.0]])\n    let y = from_data([[3.0], [5.0], [7.0], [9.0]])\n\n    let w = randn(1, 1)\n    set_requires_grad(w, true)\n    let b = zeros(1, 1)\n    set_requires_grad(b, true)\n\n    let lr = 0.01\n    for epoch in 0..100 {\n        let pred = matmul(x, w) + b\n        let loss = mse_loss(pred, y)\n\n        backward(loss)\n\n        // Gradient descent\n        let w_grad = grad(w)\n        let b_grad = grad(b)\n    }\n\n    println(\"Training complete\")\n}", "difficulty": "Advanced", "category": "ml"},
  {"name": "Pipeline Operator", "description": "Chain transformations with |> for functional style", "code": "fn double(x: i32) -> i32 { x * 2 }\nfn add_one(x: i32) -> i32 { x + 1 }\nfn square(x: i32) -> i32 { x * x }\nfn to_string(x: i32) -> str { f\"result = {x}\" }\n\nfn main() {\n    // Without pipeline\n    let result1 = to_string(square(add_one(double(5))))\n\n    // With pipeline — reads left-to-right\n    let result2 = 5 |> double |> add_one |> square |> to_string\n\n    println(result1)\n    println(result2)\n}", "difficulty": "Beginner", "category": "basics"}
]
